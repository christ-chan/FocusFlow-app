import React from 'react';
import ReactDOM from 'react-dom/client';
import SchedulingApp from './SchedulingApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<SchedulingApp />);